Board
    vector<vector<Cell>>

Cell
    enum EMPTY, X, O

User
    string anme


Game
    vector<Strategies>
    vector<Observers>
    Board
    vector Users
    GameOver() -> notify()
    AddObserver()
    Notify()
    makeMove(int row, int col, User)
    


GameObserser
    update()

Scoreboard : GameObserver

WinStrategy
    checkWinner
Column : WinStrategy
Row : WinStrategy
Diagonal : WinStrategy

GameSystem
    Scoreboard
    
    Game* createGame(user1,user2) {
        Game* game = new Game(user1,user2)
        game->AddObserver(Scoreboard);
    }

    bool makeMove(int row, int col, Game*) {
        
    }

